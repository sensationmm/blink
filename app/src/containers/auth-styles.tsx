import { InputWrapper } from "../components/styles";
import styled from "styled-components";

export const AuthWrapper = styled(InputWrapper)`
    max-width: 800px;
    margin: 50px auto;
`

export const Header = styled.header`
    text-align: center;
    background: var(--basic-white);
    padding-bottom: 20px;
`;
